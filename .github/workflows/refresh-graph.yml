name: Refresh contribution graph

on:
  schedule:
    - cron: "17 5 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Regenerate contributions.json
        run: python3 update-graph.py ishanmalu 12

      - name: Commit if it changed
        run: |
          if git diff --quiet -- contributions.json; then
            echo "unchanged"
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Refresh contribution graph" -- contributions.json
          git push
